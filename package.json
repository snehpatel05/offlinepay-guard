{
  "name": "offlinepay-guard",
  "version": "1.0.0",
  "private": true,
  "description": "Buildathon-ready offline payment prototype with Razorpay preload, signed IOUs, SQLite ledgers, and Gemini risk review.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspace server && npm run check --workspace client",
    "start": "npm run start --workspace server",
    "check": "npm run check --workspace server && npm run check --workspace client"
  }
}
